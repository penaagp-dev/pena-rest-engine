from .base import Base
import os

class Seed(Base): 
    """
usage:
    seed

Command:

Options:
-h --help                             Print usage
    """
    def execute(self):
        print("Coming Soon")
        exit(0)
